# Local dashboard secrets — copy to .streamlit/secrets.toml and fill in.
# On Streamlit Community Cloud, paste this same content into
# your app's Settings → Secrets instead (never commit the real file).

GEMINI_API_KEY = "paste_your_key_here"
ANTHROPIC_API_KEY = "sk-ant-paste_your_key_here"
